meta:
  environment: docker # Общее окружение - Docker-контейнер
  toolchain:
    name: nodejs     # А внутри Docker-контейнера используется Node.js
    version: 20

build:
  cmd: npm install && npm run build

run:
  type: http
  cmd: npx serve dist -l 4321
  containerPort: 4321