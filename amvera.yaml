meta:
  environment: nodejs # Возвращаем эту строку, убираем toolchain
  version: 20 # Указываем версию Node.js

build:
  cmd: npm install && npm run build

run:
  type: http
  cmd: npx serve dist -l 4321
  containerPort: 4321